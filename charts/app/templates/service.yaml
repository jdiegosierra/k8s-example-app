apiVersion: v1
kind: Service
metadata:
  name: {{ include "revolut-app.fullname" . }}
  labels:
    {{- include "revolut-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "revolut-app.selectorLabels" . | nindent 4 }} 