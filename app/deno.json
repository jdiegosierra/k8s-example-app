{
  "tasks": {
    "dev": "deno run --watch --allow-net --allow-env main.ts",
    "postgres": "docker run -e POSTGRES_PASSWORD=postgres -p 5432:5432 postgres",
    "local": "deno task postgres && deno task dev"
  },
  "imports": {
    "@std/assert": "jsr:@std/assert@1"
  }
}
